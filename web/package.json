{
  "name": "beeweb",
  "private": true,
  "version": "0.1.28",
  "type": "module",
  "scripts": {
    "predev": "node scripts/check-node.cjs",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "node scripts/check-node.cjs",
    "build": "vue-tsc --noEmit && vite build",
    "build:standalone": "node scripts/bump-version.mjs && npm run build && node scripts/build-standalone.mjs",
    "prepreview": "node scripts/check-node.cjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "dompurify": "^3.4.2",
    "lucide-vue-next": "^1.0.0",
    "marked": "^18.0.3",
    "vue": "^3.5.34"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "typescript": "^6.0.3",
    "vite": "^8.0.12",
    "vitest": "^3.2.7",
    "vue-tsc": "^3.2.8"
  }
}
